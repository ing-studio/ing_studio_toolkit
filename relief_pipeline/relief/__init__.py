"""Relief pipeline: point cloud + Archicad project -> relief-only Archicad project.

The output holds ONE terrain Mesh and the contour lines cut from that mesh, one layer per cut size.
Entry points: ``relief.bat`` (terminal), ``python -m relief``, the Tapir palette button (addon/).
"""
__version__ = "2.0.0"
